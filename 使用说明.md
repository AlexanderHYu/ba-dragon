# 龙区分类器 使用说明

只读游戏日志和 BATrace 的公开接口，不碰游戏内存、不注入进程、不影响反作弊。

## 装好之后第一件事

1. 打开软件，点右上角「设置」。
2. 选断箭的**游戏目录**（`...\steamapps\common\broken_arrow`），或者点「自动检测」——日志目录会自己推出来。
3. 进一局游戏，软件会自动把名单里每个人都算一遍。

顶栏那一行随时能看到：版本号、日志监听状态、BATrace 连接状态。

## 界面

主界面一页放下所有东西：当前房间/对局、玩家查询、对局档案 + 行车记录仪（并排）、卡组工具、封禁监控。
复盘是整页打开，其余都在各自的卡片里，没有侧边抽屉。

### 当前房间 / 对局

进对局就自动开查，分两轮：

1. **第一轮**拉每个人的档案（ELO、胜率、偏好兵种），大约 12 秒，列表先填满；
2. **第二轮**补龙区分，分数陆续亮起来，再约 12 秒。

先查敌方那一队。请求严格串行、间隔 1.2 秒，不给 BATrace 添乱。算好的结果存在本地，
点开某个人就是读存档，不会再发请求；想强制重查，点玩家详情里的「重新查询」。

每行显示：龙区分和评价（龙/强/中/弱/区）、ELO、K/D、胜率、样本数、常用单位。
**K/D 和胜率都取算龙区分用的那 20 场排位局**，口径和分数对得上，自定义局这类没参考价值的本来就不在里面。

点一行，详细信息开在下面的「玩家查询」卡片里：龙区分和它的可能范围、四个分项的百分位、兵种构成、
档案（ELO / 胜率 / K/D / 伤害交换比 / 花费偏好 / 最爱单位）、地图表现、调查羁绊、最近对局。
**30 分钟内查过的人直接读本地，不会再发请求**；要强制重查点「重新查询」。

> 4.0.x 分成「粗查」和「查龙区分」两个按钮、两个窗口。实测两者只差一个接口请求，
> 所以现在合并成一个：开局一次算好，不用反复点。

### 对局档案

列出打过的对局：时间、地图、模式（排位/自定义）、账号、胜负、ELO 变化、你这局的龙区分。
点一条打开整页复盘。

复盘页四个标签：

- **总览**：双方阵营、平均 ELO、赛前预期胜率、摧毁/损失/净交换、兵种构成，外加「本局要点」
- **玩家**：每个人的龙区分、称号、净交换、存活率、击杀分/花费；点开看他每个单位的出兵和阵亡
- **单位**：全场单位的使用率、死亡率、阵亡存活时间、击杀分/花费
- **时间线**：双方场上兵力曲线和每分钟损失

几个数字的口径：

- **出兵**是出动次数，飞机按架次算，返航后再出算两次。括号里的「回收」是返航/开回/开局卖掉的，
  官方全额退款，所以不算花费，但**算出兵**（它确实上场打了仗）。
- **单位的击杀分是估算**：数据里每个单位只有击杀数，没有击杀分，所以把这个人的总击杀分按各单位击杀数分下去。
  玩家那一行的击杀分是精确值。
- **同一个单位的不同配装分开统计**：名字后面带「配装 A / B」，出得多的是 A。
  BATrace 的公开接口不给配装名字（他们站内有，但没开放接口），所以暂时只能这么标；
  这样至少能看出「同样是 M1A1，这套挂载死亡率 75%、那套 50%」。
- **单位花费也是估算**：单位库只有基础价格（不含配装），所以按每个人的官方总花费等比例缩放。队伍总数和官方一致。

### 工具

- **行车记录仪**：开了之后每局自动录游戏那块屏幕，打完合成 MP4 存本地。可以选显示器、
  分辨率、帧率、码率、曝光（HDR 屏录出来偏暗时调）、录不录系统声音。
  点录像可以直接在软件里播放，进度条上画着双方的兵力曲线，方便找关键时刻；支持 0.5/1/2/4 倍速。
- **卡组**：两栏，左边前线卡组（.dek）、右边后勤仓库（.zip），和老版一样：备份选中 / 备份全部 /
  部署到前线 / 删除所选 / 打开目录。每局开始会自动把当前卡组覆盖进「上一局卡组包」。
  队友的卡组看不到——日志里只有本机玩家的，BATrace 也不提供。
- **封禁监控**：启动时查一次 BATrace 的封禁名单，只标记你遇到过的人。其余时候点「刷新」。

## 数据存在哪

`%APPDATA%\broken-arrow-log-assistant`：

| | |
|---|---|
| `settings.json` | 设置（和 4.0.x 同一个文件） |
| `dragon.sqlite` | 本地库：对局、每局每人每单位、玩家、相遇、ELO 快照、接口缓存 |
| `replays\` | 录像 |
| `deck-backups\` | 卡组备份 |

第一次启动会把 4.0.x 的对局档案、玩家库、ELO 快照和接口缓存搬进本地库，只搬一次、只新增不覆盖，
老文件原样留着。

## 自动更新

启动 8 秒后、之后每 6 小时查一次本仓库的 Release。

- **安装版**：发现新版就在后台下载，下好后点「重启更新」马上装好并重开；不点的话下次关软件时自动装。
- **免安装版**：exe 没法自己替换自己，只提示去下载页。推荐用安装版。

## 会连哪些网

只有两个：BATrace 的公开接口（运营方已同意本工具使用其 API），以及 GitHub 上本仓库的 Release 信息（查新版本）。
没有任何自建服务器，不上传你的数据。

## 常见问题

**进对局了但没查？** 看设置里日志目录对不对，底下会显示「正在监听：某个文件」。
游戏要重启一次才会写新的日志文件。

**分数为什么和我感觉不一样？** 龙区分是和**同分段、同兵种构成**的人比，不是绝对强弱。
算法和参数怎么来的见 [docs/algorithm.md](docs/algorithm.md)。

**录像是黑屏 / 没录上？** 多半是选错了显示器（要选游戏所在那块），或者显卡直采探测不到那块屏——
设置里选显示器的下拉框会标出来。录制日志在「行车记录仪」卡片底下。

**两台电脑能同步吗？** 不能，数据全在本地。把 `%APPDATA%\broken-arrow-log-assistant` 拷过去可以。
